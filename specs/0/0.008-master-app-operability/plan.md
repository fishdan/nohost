# Plan

1. Record the one-master-app rule in SpecKit and in `start.ai`, README, and AGENTS.md.
2. Change `npm run doctor` to resolve the public `/exec` URL, print it, and check current home, sign, and view pages.
3. Replace the guestbook editor authorization handoff with an owner-in-browser visit to `?page=view`.
4. Deploy the current source onto this checkout’s existing Hello World script (no `clasp create`).
5. Point GitHub production at that same script and versioned deployment ID, then verify Actions.
