# Tasks

- [x] Track latest Node with `.nvmrc` (`node`) and CI `node-version: latest` instead of a pinned major.
- [x] Include `npm doctor versions` in the environment gate.
- [x] Implement `scripts/doctor.mjs` with npm doctor subset, develop checks, clasp credential checks, and public Hello World verification.
- [x] Add `--fix` (safe remediations only) and `--ci` (toolchain-only) modes.
- [x] Update `start.ai` session startup and README local-development docs.
- [x] Run `--ci` from GitHub Actions CI.
- [x] Add tests and run `npm test`.
