# Plan

1. Inspect the current Apps Script routing and generated HTML conventions.
2. Define configuration for a private owner-controlled spreadsheet and the `Visitors` worksheet.
3. Implement server-side validation, append, and read operations with tests.
4. Build the sign and read-only viewing pages with accessible navigation and feedback.
5. With owner approval, create or connect the spreadsheet, deploy, and verify anonymous behavior.
