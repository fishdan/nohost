# Tasks

- [x] Define the spreadsheet-backed visitor guestbook requirements and boundaries.
- [x] Inspect existing application structure and create an implementation plan.
- [x] Add private spreadsheet configuration and server-side guestbook operations.
- [x] Add the sign and read-only guestbook pages.
- [x] Add automated tests for validation, storage boundaries, escaping, and pages.
- [ ] With owner approval, create or connect the spreadsheet and deploy the guestbook.
- [ ] Verify anonymous signing and read-only viewing on the public `/exec` URL.
