# Plan

1. Rewrite the README first-run around an AI prompt, nvm, doctor, standalone create, and `npm run deploy`.
2. Add a first-run procedure to `start.ai` that assistants must complete until Hello World is public.
3. Fix doctor remediations (`--type standalone`) and tests that lock the wording.
4. Clarify that first-time local `npm run deploy` is allowed for a new nohost project; GitHub production remains separate.
